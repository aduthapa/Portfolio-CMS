node_modules/
dist/
.next/
.env
*.log
npm-debug.log*
logs/
# Passenger's restart-trigger directory (cPanel Node.js Selector).
tmp/

# Uploaded media (persisted on the host, not in version control).
# The upload middleware creates these directories on demand, so nothing
# needs to be committed to keep them around.
public/uploads/**

# Editor / OS
.DS_Store
.vscode/
.idea/
